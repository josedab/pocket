# Pocket Development Environment Variables
# Copy this file to .env.local and modify as needed.
# All variables are optional — Pocket works without any configuration.

# ─── Debug Logging ────────────────────────────────────────────────
# Enable debug output for specific Pocket modules.
# Examples: "pocket:*", "pocket:sync", "pocket:storage"
# DEBUG=pocket:*

# ─── Test Server ──────────────────────────────────────────────────
# Configuration for the local sync server used during development.
# SYNC_SERVER_PORT=3001
# SYNC_SERVER_HOST=localhost

# ─── Cloud Development ───────────────────────────────────────────
# API key for testing Pocket Cloud integration (test keys only).
# POCKET_CLOUD_API_KEY=pk_test_xxxxxxxx
# POCKET_CLOUD_PROJECT_ID=proj_dev_123

# ─── AI Schema Generator ─────────────────────────────────────────
# Provider API key for the AI-powered schema generator (@pocket/codegen).
# OPENAI_API_KEY=sk-xxxxxxxx
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# ─── Node.js Memory ──────────────────────────────────────────────
# Increase heap size for running the full test suite or validation.
# Recommended for machines with 8GB+ RAM when running `pnpm validate`.
# NODE_OPTIONS="--max-old-space-size=8192"
